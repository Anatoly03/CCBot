using System;
using System.Threading.Tasks;
using EEUniverse.Library;

namespace CCBot
{
    public class Generator
    {
        public async Task Terrain()
        {
            
        }

        public async Task Sky()
        {
            /*int[] colors = { 84, 69, 91, 68, 90, 89, 67 };

            for (int y = 0; y < Program.Height; y++)
            {
                for (int x = 0; x < Program.Width; x++)
                {
                    int bid = colors[(int)Math.Floor(1d * colors.Length / Program.Width * y)];
                    await Program.PlaceBlock(0, x, y, bid);
                }
                Console.WriteLine(1d * colors.Length / Program.Width);
            }*/
        }
    }
}
